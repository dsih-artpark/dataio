[project]
name = "dataio"
version = "0.2.0"
description = "Postgres and FASTAPI based Dataset Management System (DMS)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "psycopg2>=2.9.10",
    "py-bcrypt>=0.4",
    "pyopenssl>=25.0.0",
]
